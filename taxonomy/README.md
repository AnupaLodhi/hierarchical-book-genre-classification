# Taxonomies

This directory contains the taxonomies used by the hierarchical book genre classification pipeline.

## Files

- `genre_hierarchy.json` — hierarchical taxonomy used for book genre classification.
- `metadata_hierarchy.json` — separate hierarchy for book metadata organization.

Genre classification and metadata classification are intentionally maintained as separate taxonomies.

These taxonomy files are version-controlled to ensure reproducible experiments.
